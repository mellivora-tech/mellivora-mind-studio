{
  "name": "mms-agent-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "lint": "biome check src"
  },
  "dependencies": {
    "@voltagent/core": "^0.1.0",
    "@voltagent/vercel-ai": "^0.1.0",
    "@ai-sdk/openai": "^1.0.0",
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
