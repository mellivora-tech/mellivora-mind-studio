version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: mellivora-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mellivora
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations/postgres:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  etl-config:
    build:
      context: ./services/etl-config
      dockerfile: Dockerfile
    container_name: mellivora-etl-config
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: mellivora
      DB_SSLMODE: disable
      PORT: 8080
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy

  etl-engine:
    build:
      context: ./compute/etl-engine
      dockerfile: Dockerfile
    container_name: mellivora-etl-engine
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: mellivora
      DB_SSLMODE: disable
      SERVICE_PORT: 9106
      SCHEDULER_ENABLED: "true"
      SCHEDULER_POLL_INTERVAL: 60
      LOG_LEVEL: INFO
      TUSHARE_TOKEN: ${TUSHARE_TOKEN:-}
    ports:
      - "9106:9106"
    depends_on:
      postgres:
        condition: service_healthy
      etl-config:
        condition: service_started

volumes:
  postgres_data:
